#ifndef __FAN_H
#define __FAN_H
#include "main.h"

typedef struct Fan
{
	GPIO_TypeDef* port;
	uint16_t pin;
} Fan_t;

extern Fan FAN1;
extern Fan FAN2;
extern Fan FAN3;
extern Fan FAN4;

void Driver_Fan_On(Fan);
void Driver_Fan_Off(Fan);
void Driver_Fan_Toggle(Fan);
void Driver_Fan_All_On();
void Driver_Fan_All_Off();
void Driver_Fan_All_Toggle();

#endif


